[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "22"

# SvelteKit runs client-side; unknown paths fall through to the SPA shell.
[[redirects]]
  from = "/*"
  to = "/200.html"
  status = 200
